import { createChatBotMessage } from 'react-chatbot-kit';
const config = {
  botName: "Mr.Fire Fighter",
  initialMessages: [createChatBotMessage(`Hello...How can I help you`)],
};

export default config;